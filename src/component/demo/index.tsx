import React from "react";
//có thể viết nhanh một component bằng phím tắt "rfce" hoặc "rfc"
function Demo() {
  //Một component được tạo như 1 function
  //code JavaScript || TypeScript xử lí logic vd : hàm login , click submit form , call API ...
  return (
    <div>Demo</div> // Trong hàm return sẽ trả về(render) ra một đoạn code HTML
  );
}

export default Demo; // export ra để  dùng ở các file khác
